# API base URL of the Aether backend
VITE_API_BASE=http://localhost:8000

# Backend engine: "1" enables Groq-aware UI (engine badge, runtime panel, faster
# status polling). Set to "0" or "false" to fall back to the legacy Gemini
# display mode.
VITE_GROQ_BACKEND=1
